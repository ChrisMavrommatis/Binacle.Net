# See https://docs.github.com/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# Compiled output
bin/
obj/

# Visual Studio
.vs
*.user

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# Rider
.idea

# Node
node_modules

# Project Specific
# Build-time OpenAPI documents (emitted by OpenApiGenerateDocumentsOnBuild); the frozen per-version specs
# under docs/collections/_versions/**/swagger/ stay the committed source of truth.
build/openapi/
data/logs/
data/pack-logs/
packing-logs/
BenchmarkDotNet.Artifacts
PerformanceTestsArtifacts
PerformanceTests.Artifacts
coverage/
Output
output
# Published app, copied into the image by the Dockerfile. The generic "output" rule above no longer
# covers it now that it is named after what it holds.
build/binacle-net/
# Per-suite test results and coverage, written by `just test` / `just coverage`.
build/tests/
build/coverage/

## Copied from UI Module
src/Binacle.Net/wwwroot/ 

## data
azurite/
config/minio_data/
config/postgres/
config/data/

*.db
*.sqlite
*.ndjson

## Jekyll
_site
.sass-cache
.jekyll-cache
.jekyll-metadata

## Docs
build/docs
docs/js
docs/media
docs/lib
docs/*.ico
docs/*.png

## Website
build/web
web/js
web/media
web/lib
web/*.ico
web/*.png

## Agent session prompts
# Hand-off prompts for delegated sessions. Deliberately untracked: they name deployment hosts and are
# throwaway scaffolding for one run, where the durable brief is the plan file under .agents/plans/.
.agents/prompts/
