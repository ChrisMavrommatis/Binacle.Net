<Project Sdk="Microsoft.NET.Sdk">

	<ItemGroup>
		<InternalsVisibleTo Include="$(ProjectName).UnitTests" />
		<!-- The generator tool drives the internal EncodingInfoNotation to produce golden vectors. -->
		<InternalsVisibleTo Include="$(ProjectName).Generators" />
	</ItemGroup>

</Project>
