# Assets

Shared static assets — favicons, touch icons, and app icons used across the sites.

At build time these are copied into [`docs/`](../docs) and [`web/`](../web) by the gulp tasks — run them with
`just assets` from the repo root. Edit the originals here, not the copies.
