# Assets

Shared static assets — favicons, touch icons, and app icons used across the sites.

At build time these are copied into [`docs/`](../docs) and [`web/`](../web) by the gulp tasks
(`npm run copy-assets-to-docs`, `npm run copy-assets-to-web`). Edit the originals here, not the copies.
