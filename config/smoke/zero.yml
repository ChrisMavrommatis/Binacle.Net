name: binacle-smoke-zero

# Smoke profile `zero` - as shipped, no env at all. One claim: the image boots from nothing and a core route
# answers. No backend, no volume - nothing to persist.

services:
  binacle-net:
    image: binacle-net:local
    ports:
      - "8080:8080"
